doctype html
html
  head
    link(rel="stylesheet", href="/stylesheets/style.css")
    link(rel="stylesheet", href="/stylesheets/newVoting.css")
    include partials/appName
  body
    div(class="background")
      div(class="container")
        p(class="newVotingMessage")= message
        form(
          class="newVotingForm"
          action="/votings/new"
          method="POST"
        )
          input(
            class="votingTitle"
            type="text"
            name="title"
            placeholder="투표 주제를 입력하세요"
            maxlength="25"
          )
          span(class="votingTitleText") 투표 주제는 25자 이하로 입력할 수 있습니다.

          input(
            class="expiredAt"
            type="datetime-local"
            name="expiredAt"
          )
          span(class="expiredAtText") 투표 마감시간을 선택해주세요

          div(class="optionRow")
            input(
              class="option"
              type="text"
              name="options"
              maxlength="25"
            )
            button(
              class="removeOptionBtn"
              type="button"
              alt="remove"
            ) ➖
          div(class="optionRow")
            input(
              class="option"
              type="text"
              name="options"
              maxlength="25"
            )
            button(
              class="removeOptionBtn"
              type="button"
              alt="remove"
            ) ➖
          p(class="addOptionMessage") 보기는 최대 5개까지만 만들 수 있습니다
          div(class="buttons")
            button(class="addOptionBtn" type="button") 보기 추가
            button(class="createVotingBtn" type="submit") 투표 만들기
        a(href="/")
          button(class="backToMainBtn" type="button") 메인으로 돌아가기

      script(src="/javascripts/newVoting.js")
