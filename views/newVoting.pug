doctype html
html
  head
    link(rel="stylesheet", href="/stylesheets/style.css")
    link(rel="stylesheet", href="/stylesheets/newVoting.css")
    include partials/appName
  body
    div(class="background")
      div(class="container")
        form(
          class="newVotingForm"
          action="/votings/new"
          method="POST"
        )
          input(
            class="votingTitle"
            type="text"
            name="title"
            placeholder="투표 주제를 입력하세요"
          )
          span(class="votingTitleText") 30자 이하로 입력해주세요

          input(
            class="expiredAt"
            type="date"
            name="expiredAt"
            placeholder="투표 마감시간을 선택하세요"
          )
          span(class="expiredAtText") 투표 마감시간을 선택해주세요

          div(class="optionRow")
            input(
              class="option"
              type="text"
              name="option"
            )
            button(
              class="removeOptionBtn"
              type="button"
              alt="remove"
            ) ➖
        div(class="buttons")
          button(class="addOptionBtn" type="button") 보기 추가
          button(class="createVotingBtn" type="submit") 투표 만들기

      script(src="/javascripts/newVoting.js")
