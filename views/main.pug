doctype html
html
  head
    link(rel="stylesheet", href="/stylesheets/style.css")
    link(rel="stylesheet", href="/stylesheets/main.css")
    include partials/appName
  body
    .background
      .container
        header.mainHeader
          a(href="/myVotings")
            button.myVotingBtn(type="button" alt="profile") 🙍‍♀️
          span.mainTitle BLUE VOTE
          a(href="/votings/new")
            button.newVotingBtn(type="button" alt="createNewVote") ➕
        p.mainSubTitle 당신의 투표권을 행사하세요 !

        nav.selectButtons
          a(href="/ongoing")
            button.ongoingVoteBtn(type="button") 진행중인 투표
          a(href="/finished")
            button.finishedVoteBtn(type="button") 마감된 투표

        section.votingSection
          each vote in votes
            a(href=`/votings/${vote._id}`)
              .votingContainer
                .votingInfo
                  span.creator= vote.createUser.email
                  span.expiredTime #{vote.expiredAt.toLocaleString()}
                p.votingTitle= vote.title
        if isFiltered
          a(href="/")
            button.backToMainBtn(type="button") 모든 투표 보기
