doctype html
html
  head
    link(rel="stylesheet", href="/stylesheets/style.css")
    link(rel="stylesheet", href="/stylesheets/main.css")
    include partials/appName
  body
    .background
      .container
        header.mainHeader
          a(href="/myVotings")
            button.myVotingBtn(type="button" alt="profile") 🙍‍♀️
          span.mainTitle BLUE VOTE
          a(href="/votings/new")
            button.newVotingBtn(type="button" alt="createNewVote") ➕
        p.mainSubTitle #{email.split('@')[0]} 님이 만든 투표리스트를 확인해보세요 !

        section.votingSection
          each vote in votes
            a(href=`/votings/${vote._id}`)
              .votingContainer
                .votingInfo
                  span.creator= email
                  span.expiredTime #{vote.expiredAt.toLocaleString()}
                p.votingTitle= vote.title
        a(href="/")
          button.backToMainBtn(type="button") 메인으로 돌아가기
